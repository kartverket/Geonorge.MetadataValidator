<!DOCTYPE html>
<html lang="no">
    <head>
        <meta charset="utf-8">
        <title>@ViewBag.Title - Kartverket</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="">
        <meta name="author" content="">

        @Styles.Render("~/Content/css")
        @Styles.Render("~/Content/bootstrap/css/bundle")

        @RenderSection("Head", required: false)

        @Scripts.Render("~/bundles/modernizr")

        <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
        <!--[if lt IE 9]>
        <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
        <![endif]-->

        <style type="text/css">
            body {
                background-color: #fff;
            }
            .site-logo { float: right;}
            .site-logo img { 
                height: 40px;
                float: left;
                margin: 5px;
            }
            .site-logo p {
                font-size: 1.2em; 
                margin-top: 20px;
            }
        </style>
    </head>
    <body>
        
        <div class="container">
            
            <div class="site-logo">
                <img src="~/Content/logo-kartverket.png" width="118" height="172" alt="Logo Kartverket"/>
                <p>Kartverket</p>
            </div>

            <h1>Metadata Monitor - @ViewBag.Title</h1>

            @RenderBody()

            <hr>
            <footer>
                <p>&copy; Kartverket @DateTime.Now.Year</p>
            </footer>
        </div>        
        @Scripts.Render("~/bundles/jquery")
        @Scripts.Render("~/Content/bootstrap/js/bundle")
        @RenderSection("scripts", required: false)
    </body>
</html>
